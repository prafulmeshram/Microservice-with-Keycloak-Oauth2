server:
  port: 8081

spring:
  application:
    name: eureka-server




eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    fetch-registry: false
    register-with-eureka: false